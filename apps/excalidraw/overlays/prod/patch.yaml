# renovate: depName=excalidraw/excalidraw datasource=docker
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: excalidraw
spec:
  values:
    app:
      image:
        tag: sha-4bfc5bb
      replicaCount: 1
    ingress:
      enabled: true
      host: draw.shakir.cloud
      ingressClassName: traefik
