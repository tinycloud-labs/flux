apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml

configMapGenerator:
  - name: gitea-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true
